import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ca.roncreator.trackmymeals',
  appName: 'Трекер харчування',
  webDir: 'dist'
};

export default config;
