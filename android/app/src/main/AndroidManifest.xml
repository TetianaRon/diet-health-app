<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Google sign-in redirect (system browser + PKCE) — see the auth
                 comment at the top of src/lib/sheets.ts. NATIVE_REDIRECT_URI
                 there is "ca.roncreator.trackmymeals:/oauth2redirect" (a
                 single slash, no host/authority component), so this filter
                 matches on scheme alone — the same convention Google's own
                 AppAuth-Android library uses, since a host+path filter can't
                 match a host-less custom-scheme URI. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ca.roncreator.trackmymeals" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Meal-time reminder (@capacitor/local-notifications): POST_NOTIFICATIONS is
         required at runtime on Android 13+ (requested via initMealReminders() in
         src/lib/reminderScheduler.ts); SCHEDULE_EXACT_ALARM is required on Android 12+
         for the reminder to fire at the exact computed time rather than a batched,
         possibly-delayed one — see the Android 12+ exact-alarm caveat in
         docs/build-log.md's 2026-09-09 reminder-scoping entry. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
</manifest>
